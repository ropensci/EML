# Sample .travis.yml for R projects

language: r
r:
  - oldrel
  - release
  - devel
warnings_are_errors: true
sudo: required

#r_github_packages:
#- jimhester/covr
#- hadley/testthat

after_success:
- Rscript -e 'library(covr); codecov()'
